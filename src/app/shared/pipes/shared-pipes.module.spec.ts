import { SharedPipesModule } from './shared-pipes.module';

describe('SharedPipesModule', () => {
  it('should create', () => {
    const mod = new SharedPipesModule();
    expect(mod).toBeTruthy();
  });
});
