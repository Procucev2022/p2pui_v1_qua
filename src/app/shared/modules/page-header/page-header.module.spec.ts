import { PageHeaderModule } from './page-header.module';

describe('PageHeaderModule', () => {
  it('should create', () => {
    const mod = new PageHeaderModule();
    expect(mod).toBeTruthy();
  });
});
